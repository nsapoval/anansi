% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{restrict_taxa}
\alias{restrict_taxa}
\title{Restrict networks to a subset of taxa (reticulation-aware pruning)}
\usage{
restrict_taxa(x, keep, validate = TRUE)
}
\arguments{
\item{x}{An \link{anansi_netset} or a single \link{anansi_network}.}

\item{keep}{Character vector of taxa to keep (>= 2 must be present).}

\item{validate}{Passed to \link{anansi_netset} when \code{x} is a set.}
}
\value{
The same class as \code{x}, restricted to \code{keep}.
}
\description{
Prunes every network to the taxa in \code{keep}. The backbone tree is pruned with
\link[ape:drop.tip]{ape::keep.tip} (suppressing degree-2 nodes); each reticulation is then
re-located by the MRCA of the surviving tips of its donor and recipient
clades, and dropped if either endpoint loses all its tips, the endpoints
collapse to the same node, or one clade nests within the other (see
docs/DESIGN.md D7).
}
\examples{
a <- parse_network("(((A:1,B:1):1,(C:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
b <- parse_network("(((A:1,C:1):1,(B:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
ns <- anansi_netset(list(a, b))
restrict_taxa(ns, c("A", "B", "C"))
}

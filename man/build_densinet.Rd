% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{build_densinet}
\alias{build_densinet}
\title{Build a densinet() figure from a flat parameter list (web-app helper)}
\usage{
build_densinet(netset, params = list())
}
\arguments{
\item{netset}{An \link{anansi_netset} (see \link{read_networks_csv} /
\link{netset_from_enewick}).}

\item{params}{Named list of \link{densinet} arguments. Recognized names mirror the
\link{densinet} formals; unknown names are ignored.}
}
\value{
A \code{ggplot} object (as returned by \link{densinet}).
}
\description{
Maps a flat list of UI parameters (e.g. Shiny inputs) onto a \link{densinet} call,
so the app server stays a one-liner and the input -> argument mapping is
unit-testable on its own. Empty selectors and blank text are treated as unset
and fall through to the \link{densinet} defaults.
}
\details{
Any \code{outgroup} taxa are force-kept in the \code{keep} subset: an outgroup that is
pruned away would make the internal tip-ordering fail to pin it, so
\code{keep <- union(keep, outgroup)}.
}
\examples{
ns <- read_networks_csv(anansi_example())
p <- build_densinet(ns, list(method = "first", reticulation_style = "hybrid"))
}
\seealso{
\link{run_anansi_app}, \link{densinet}
}

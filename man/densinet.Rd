% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{densinet}
\alias{densinet}
\title{DensiTree-style consensus/discrepancy overlay of a set of networks}
\usage{
densinet(
  x,
  tip_order = NULL,
  method = "mode",
  outgroup = NULL,
  outgroup_position = c("top", "bottom"),
  snap_to_consensus = FALSE,
  mode = c("cladogram", "phylogram"),
  layout = c("slanted", "rectangular"),
  keep = NULL,
  top_n = NULL,
  top_by = NULL,
  tree_color = "steelblue",
  ret_color = "firebrick",
  alpha = NULL,
  ret_alpha = NULL,
  linewidth = 0.3,
  jitter = 0,
  ret_linetype = "dashed",
  consensus = TRUE,
  consensus_p = 0.5,
  color_by_support = TRUE,
  consensus_color = "black",
  consensus_linewidth = 0.7,
  consensus_ret = TRUE,
  consensus_ret_min = 0.1,
  consensus_ret_color = "darkred",
  ret_edge_frac = 0.1,
  reticulation_style = c("arrow", "hybrid"),
  tip_labels = TRUE,
  tip_size = 3,
  tip_offset = 0.02,
  color_by = NULL,
  color_palette = "viridis",
  color_direction = 1,
  color_low = "grey80",
  color_high = "firebrick",
  color_legend = NULL,
  color_trim = NULL,
  color_trim_action = c("clamp", "drop"),
  backbone = NULL,
  backbone_color = "black",
  backbone_ret_color = "darkred",
  backbone_linewidth = NULL,
  consistent_only = TRUE,
  title = NULL
)
}
\arguments{
\item{x}{An \link{anansi_netset} (see \link{read_networks_csv}).}

\item{tip_order}{Optional explicit tip order; otherwise computed via
\link{consensus_tip_order} using \code{method}.}

\item{method}{Tip-ordering method (see \link{consensus_tip_order}); default \code{"mode"}.}

\item{outgroup}{Optional taxa to pin to one end of the figure (see
\link{consensus_tip_order}).}

\item{outgroup_position}{\code{"top"} (default) or \code{"bottom"} placement for
\code{outgroup}.}

\item{snap_to_consensus}{If TRUE, snap the shared tip order to the consensus
topology so the consensus backbone has no crossings (see
\link{consensus_tip_order}). Default FALSE.}

\item{mode}{\code{"cladogram"} (default) or \code{"phylogram"}.}

\item{layout}{\code{"slanted"} (default) or \code{"rectangular"} for the cloud backbone.}

\item{keep}{Optional character vector of taxa; if given, all networks are
restricted to this subset via \link{restrict_taxa} before plotting.}

\item{top_n, top_by}{If \code{top_n} is set, restrict to the top-N networks by the
metadata column \code{top_by} (see \link{top_networks}) before plotting.}

\item{tree_color, ret_color}{Colors for the backbone and reticulation clouds.}

\item{alpha, ret_alpha}{Per-edge cloud opacities; default to roughly \code{1/N} and
\code{2/N} so consensus accumulates while conflict stays faint. When \code{color_by}
is set, \code{alpha} instead defaults to \code{max(3/N, 0.25)} so the gradient reads.}

\item{linewidth}{Cloud edge width.}

\item{jitter}{Max per-network x-offset to separate overlapping edges
(DensiTree-style); 0 (default) disables it.}

\item{ret_linetype}{Line type for reticulation edges (default \code{"dashed"}).}

\item{consensus}{If TRUE (default), draw the consensus backbone + reticulations.}

\item{consensus_p}{Majority threshold for the consensus tree (default 0.5).}

\item{color_by_support}{If TRUE (default), color the consensus backbone by
clade support (grey -> \code{consensus_color}).}

\item{consensus_color, consensus_linewidth}{Color/width of the consensus backbone.}

\item{consensus_ret}{If TRUE (default), draw consensus reticulation arrows.}

\item{consensus_ret_min}{Minimum event frequency to draw a consensus
reticulation arrow (default 0.1).}

\item{consensus_ret_color}{Color of consensus reticulation arrows/edges.}

\item{ret_edge_frac}{Fraction (0-1) of the way from each reticulation
endpoint's node toward its parent at which to anchor the edge, so
reticulations attach along a branch rather than at the tip/MRCA node.
Default 0.1; 0 reproduces at-the-node anchoring.}

\item{reticulation_style}{\code{"arrow"} (default) draws each consensus
reticulation as a directed donor -> recipient arrow; \code{"hybrid"} keys events
direction-agnostically and draws two undirected dotted edges into the hybrid
node (so direction-unstable reticulations display identically).}

\item{tip_labels}{Whether to draw tip labels.}

\item{tip_size, tip_offset}{Tip-label text size and x-offset past the tips.}

\item{color_by}{Optional name of a numeric metadata column (see
\link{read_networks_csv}); when set, each tree in the cloud is colored by its
value along a gradient. To keep a single color scale, the scaffold is then
drawn solid (consensus \code{color_by_support} is auto-disabled).}

\item{color_palette}{Gradient for \code{color_by}: one of the viridis options
\code{"viridis"} (default), \code{"magma"}, \code{"plasma"}, \code{"inferno"}, \code{"cividis"}, or
\code{"gradient"} for a two-color \code{color_low} -> \code{color_high} ramp.}

\item{color_direction}{Viridis direction (\code{1} or \code{-1}); ignored for
\code{"gradient"}.}

\item{color_low, color_high}{Endpoints of the \code{"gradient"} palette.}

\item{color_legend}{Legend title for \code{color_by} (defaults to the column name).}

\item{color_trim}{Strong-outlier handling for \code{color_by}. A non-negative IQR
multiplier \code{k}: per-network values outside the Tukey fences
\verb{[Q1 - k*IQR, Q3 + k*IQR]} are treated as outliers (\code{3} ~ "strong", \code{1.5} ~
"mild"). \code{NULL} (default) disables trimming.}

\item{color_trim_action}{What to do with \code{color_trim} outliers: \code{"clamp"}
(default) keeps every tree but pins the color scale to the robust range so
outliers saturate at the end color; \code{"drop"} removes the outlier networks
from the overlay.}

\item{backbone}{Optional explicit scaffold drawn solid in place of the
consensus backbone: a Newick / extended-Newick string, \link{anansi_network},
\code{evonet}, or \code{phylo} over (a subset of) the figure's taxa. If it is a
network, its reticulation edges are drawn too.}

\item{backbone_color, backbone_ret_color, backbone_linewidth}{Colors of the
explicit \code{backbone}'s tree and reticulation edges, and their width (width
defaults to \code{consensus_linewidth}).}

\item{consistent_only}{If TRUE (default), drop networks with divergent taxa.}

\item{title}{Plot title; a default is generated if NULL.}
}
\value{
A \code{ggplot} object.
}
\description{
Overlays every network in a set on one shared tip order with alpha
transparency: the backbone trees form a "consensus cloud" (agreement is dense,
conflict is diffuse) and the reticulation edges form a distinct cloud. On top,
a solid consensus ("root-canal") backbone is drawn -- colored by clade support
-- together with the frequent reticulation events (donor -> recipient arrows),
so the dominant signal is unambiguous regardless of \code{alpha}. This is anansi's
primary view (DensiTree adapted to networks; see docs/DESIGN.md section 7 and
D4/D5). Returns a composable \code{ggplot} object.
}
\examples{
a <- parse_network("(((A:1,B:1):1,(C:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
b <- parse_network("(((A:1,B:1):1,(C:1)#H1:1::0.7):1,(D:1,#H1:1::0.3):1);")
p <- densinet(anansi_netset(list(a, b)), method = "first")
}
\seealso{
\link{layout_netset}, \link{consensus_network}, \link{consensus_tip_order},
\link{top_networks}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_netset}
\alias{layout_netset}
\title{Lay out a whole set of networks on one shared tip order}
\usage{
layout_netset(
  x,
  tip_order = NULL,
  method = "mode",
  mode = "cladogram",
  scale_x = TRUE,
  jitter = 0,
  consistent_only = TRUE
)
}
\arguments{
\item{x}{An \link{anansi_netset}.}

\item{tip_order}{Optional explicit tip order; if NULL it is computed via
\link{consensus_tip_order} with \code{method}.}

\item{method}{Tip-ordering method passed to \link{consensus_tip_order}.}

\item{mode}{\code{"cladogram"} (default) or \code{"phylogram"}.}

\item{scale_x}{If TRUE (default), rescale each network's x to \verb{[0, 1]}
(root = 0, tips = 1) so tips align across networks.}

\item{jitter}{Max per-network x-offset (evenly spread over \verb{[-jitter, jitter]})
to separate exactly overlapping edges; 0 (default) disables it.}

\item{consistent_only}{If TRUE (default), drop networks with \code{taxa_ok = FALSE}.}
}
\value{
A data.frame of segments (as \link{layout_network}) with an added integer
\code{.net} column; the chosen order is attached as \code{attr(., "tip_order")}.
}
\description{
Applies \link{layout_network} to every network in the set using a common tip
order, and (by default) normalizes each network's x to \verb{[0, 1]} so backbones
overlay. Networks whose taxon set diverges (\code{taxa_ok = FALSE}) are dropped
with a warning by default. See docs/DESIGN.md (D2).
}
\examples{
a <- parse_network("(((A:1,B:1):1,(C:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
b <- parse_network("(((A:1,C:1):1,(B:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
seg <- layout_netset(anansi_netset(list(a, b)), method = "first")
attr(seg, "tip_order")
}

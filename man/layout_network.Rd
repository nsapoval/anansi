% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_network}
\alias{layout_network}
\title{Lay out one network on a shared tip order}
\usage{
layout_network(net, tip_order, mode = c("cladogram", "phylogram"))
}
\arguments{
\item{net}{An \link{anansi_network} or ape \code{evonet}/\code{phylo}.}

\item{tip_order}{Character vector giving the shared tip order.}

\item{mode}{\code{"cladogram"} (default, uniform depths) or \code{"phylogram"}
(branch-length scaled).}
}
\value{
A data.frame of segments with columns \code{from}, \code{to}, \code{x}, \code{y}, \code{xend},
\code{yend}, \code{kind} (\code{"tree"}/\code{"reticulation"}), \code{is_tip}, \code{label}.
}
\description{
Computes node coordinates on a fixed tip order and returns a tidy table of
drawable segments (backbone tree edges and reticulation edges). Tips are
pinned to their rank in \code{tip_order} (shared across networks); internal-node y
is the mean of its children; x is the backbone node depth (tip-aligned for
\code{"cladogram"}, branch-length-scaled for \code{"phylogram"}). See docs/DESIGN.md
(D1, D2).
}
\examples{
a <- parse_network("(((A:1,B:1):1,(C:1)#H1:1::0.6):1,(D:1,#H1:1::0.4):1);")
seg <- layout_network(a, tip_order = c("A", "B", "C", "D"))
head(seg)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{run_anansi_app}
\alias{run_anansi_app}
\title{Launch the anansi Shiny web app}
\usage{
run_anansi_app(...)
}
\arguments{
\item{...}{Passed to \link[shiny:runApp]{shiny::runApp} (e.g. \code{port}, \code{launch.browser}, \code{host}).}
}
\value{
Invisibly \code{NULL}; called for the side effect of running the app.
}
\description{
Opens an interactive web interface for building a \link{densinet} figure: upload a
CSV of extended-Newick networks (see \link{read_networks_csv}), paste/upload raw
extended-Newick strings (see \link{netset_from_enewick}), or load the bundled
example (\link{anansi_example}); tune the key and advanced controls; preview the
figure; and download it as PNG/PDF (or SVG when \pkg{svglite} is installed).
}
\details{
Requires the suggested package \pkg{shiny}. A richer UI also uses
\pkg{bslib} (layout) and \pkg{colourpicker} (colour inputs); when these are
absent the app degrades to a plain layout and text colour fields.
}
\examples{
if (interactive() && requireNamespace("shiny", quietly = TRUE)) {
  run_anansi_app()
}
}
\seealso{
\link{build_densinet}, \link{densinet}
}

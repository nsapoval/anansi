% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_networks_csv}
\alias{read_networks_csv}
\title{Read a set of networks from a CSV of extended-Newick strings}
\usage{
read_networks_csv(path, network_col = NULL, validate = TRUE, ...)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{network_col}{Optional name of the network column (auto-detected if NULL).}

\item{validate}{If TRUE (default), warn when the taxon set is not fixed.}

\item{...}{Passed to \link[utils:read.table]{utils::read.csv}.}
}
\value{
An \link{anansi_netset}.
}
\description{
Reads a CSV in which one column holds extended-Newick network strings (named
\code{network}, \code{newick}, or \code{enewick}, auto-detected) and the remaining columns
are per-network metadata. Returns an \link{anansi_netset}. Networks whose taxon set
diverges from the modal set (e.g. parsing artifacts on nested reticulations)
are kept but flagged via \code{taxa_ok}; filter with \code{ns[ns$taxa_ok]}.
}

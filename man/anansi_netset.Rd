% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netset.R
\name{anansi_netset}
\alias{anansi_netset}
\title{Construct a set of networks over a fixed taxon set}
\usage{
anansi_netset(networks, meta = NULL, validate = TRUE)
}
\arguments{
\item{networks}{A list of \link{anansi_network} objects (or a single one).}

\item{meta}{Optional data.frame of per-network metadata (one row per network).}

\item{validate}{If TRUE (default), warn when the taxon set is not fixed.}
}
\value{
An \code{anansi_netset} with fields \code{networks}, \code{taxa} (the modal set),
\code{taxa_ok} (logical per network), and \code{meta}.
}
\description{
Networks whose taxon set diverges from the modal set are kept but flagged in
\code{taxa_ok} (filter with \code{ns[ns$taxa_ok]}); with \code{validate = TRUE} a warning
summarizes how many diverge.
}
\seealso{
\link{read_networks_csv}, \link{taxa_consistency}
}

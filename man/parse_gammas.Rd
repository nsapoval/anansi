% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{parse_gammas}
\alias{parse_gammas}
\title{Parse gamma inheritance probabilities from an extended-Newick string}
\usage{
parse_gammas(enewick)
}
\arguments{
\item{enewick}{A single extended-Newick string.}
}
\value{
A data.frame with columns \code{label}, \code{tree_gamma} (major edge) and
\code{ret_gamma} (minor edge); zero rows if there are no hybrids.
}
\description{
\code{ape::read.evonet()} parses the network \emph{structure} but does not populate the
gamma (inheritance) probabilities, so we recover them from the raw string.
Each hybrid \verb{#Hk} appears twice: the first occurrence is on the major (tree)
edge, the second on the minor (reticulation) edge. This generalizes the
reference \code{plot_network.R} logic to an arbitrary number of hybrids.
}
\examples{
parse_gammas("((A:1,(B:1)#H1:1::0.7):1,(C:1,#H1:1::0.3):1);")
}

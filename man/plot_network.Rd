% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_network}
\alias{plot_network}
\title{Plot a single phylogenetic network}
\usage{
plot_network(
  x,
  cladogram = TRUE,
  layout = "slanted",
  tip_size = 3,
  show_gamma = TRUE,
  title = NULL
)
}
\arguments{
\item{x}{An \link{anansi_network} (preferred, enables gamma labels) or an ape
\code{evonet}/\code{phylo}.}

\item{cladogram}{If TRUE (default) drop branch lengths so node depths are
uniform (topology-only view).}

\item{layout}{\code{ggevonet} layout, e.g. "slanted" (default) or "rectangular".}

\item{tip_size}{Tip-label text size.}

\item{show_gamma}{If TRUE (default) annotate gamma values (needs an
\code{anansi_network}).}

\item{title}{Plot title; if NULL a title is derived from metadata.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders one network with \code{tanggle::ggevonet()} and (for an \link{anansi_network})
annotates each hybrid edge with its gamma inheritance probability: the minor
(reticulation) edge in blue and the major (tree) edge in red. This is the
framework version of the reference \code{plot_network.R}.
}
\examples{
net <- parse_network("((A:1,(B:1)#H1:1::0.7):1,(C:1,#H1:1::0.3):1);")
p <- plot_network(net)   # a ggplot object; print(p) to draw it
}
